import React from 'react';
import Hero from "@/modules/hero";
import Features from "../modules/features";

const Page = () => {
    return (
        <div>
            <Hero/>
            <Features/>
        </div>
    );
};

export default Page;